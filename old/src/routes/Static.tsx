import { RouteComponentProps } from "@reach/router";
import { FC } from "react";

const Static: FC<RouteComponentProps> = (props) => (
  <div className="text-red-400">user</div>
);

export default Static;
